# 소스코드 비공개 — 배포판(Release)만 공개
*
!.gitignore
!README.md
!VERSION
!CHANGELOG.md

# 민감정보: 텔레그램 API 자격증명/세션/환경변수 (절대 커밋 금지)
# (상단 `*` 규칙으로 이미 제외되지만, 의도를 명시한다)
telegram_api_keys.py
*.session
*.session-journal
.env
